#include "main.h"
/**
 * add -  adds two integers
 *
 * @i: int to be added to j
 * @j: int to be added to i
 * Return: result
 */
int add(int i, int j)
{
	int result;

	result = i + j;
	return (result);
}
